import { PageHeader } from "@/components/page-frame";

/** What the directory opens with, drawn by the page and its loading state. */
export function HomeHeader() {
  return (
    <PageHeader
      title="Path of Exile tools"
      description="Every Path of Exile tool worth having, in one list. Three of them are built here."
    />
  );
}
