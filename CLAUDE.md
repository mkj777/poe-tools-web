@AGENTS.md

# Never use em-dashes

No em-dash (—) anywhere: UI copy, comments, commit messages, docs, chat replies.
Rewrite the sentence instead. A comma, a colon, a full stop or brackets always
covers it. En-dashes (–) stay allowed for ranges and as a "no value" placeholder.
